#include<stdio.h>
#include<stdlib.h>
#define tam 4
int main ()
{
    int codigos[tam],total_estoque[tam;]
    int cod_cliente,cod_produto,quant;
    int cod_existe=0;
    for (int i=0;i<tam;i++)
    {
        printf("Codigo %d: ",i);
        scanf("%d",&codigos[i]);
        printf("Estoque %d: ",i);
        scanf("%d",&total_estoque[i]);
    }

    do {
        printf("Digite o codigo do cliente: ");
        scanf("%d",&cod_cliente);
        if
        printf("Digite o codigo do produto: ");
        scanf("%d",&cod_produto);
        printf("Digite a quantidade: ");
        scanf("%d",&quant);
    }while(cod_cliente!=0);

    return 0;
}

